package com.rafario.lahrecetah.ui.theme

import androidx.compose.ui.graphics.Color

/* ---------- LIGHT COLOR SCHEME (Material 3) ---------- */

// 🌸 Primary – Melocotón pastel (acciones principales)
val Primary = Color(0xFFFF9F9F)
val OnPrimary = Color(0xFFFFFFFF)

// 🌿 Secondary – Verde salvia (ingredientes, sliders, acentos suaves)
val Secondary = Color(0xFF9DB8A0)
val OnSecondary = Color(0xFF1E2B22)

// 🧈 Tertiary – Crema / vainilla (detalles)
val Tertiary = Color(0xFFFFE3B3)
val OnTertiary = Color(0xFF3A2E1F)

// 🌸 Background – Rosa muy claro
val AppBackground = Color(0xFFFFF6F6)

// 🧱 Surface – Cards y secciones
val Surface = Color(0xFFFFFFFF)
val SurfaceVariant = Color(0xFFFFECEC)

// 🧾 Modal / BottomSheet
val ModalBackground = Color(0xFFFFE0E0)

// ❌ Error – Coral suave
val Error = Color(0xFFE57373)
val OnError = Color(0xFFFFFFFF)

// 🧭 Bottom bar
val BottomBarBackground = Color(0xFFFFFAFA)
val BottomBarIconActive = Primary
val BottomBarIconInactive = Color(0xFFB0B0B0)